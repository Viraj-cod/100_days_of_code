n = int(input("Enter the number:"))
add=0
for i in range(2,n):
    count=0
    for j in range(2,n):
        if i!=j and i % j == 0:
            count += 1
    if count == 0:
        add+=i
print(add)
